parameter (nat %target);
storage   (option nat);
code
  {
    CAR;        # @parameter
    # == target ==
    # self.data.last = sp.some(params) # @parameter
    SOME;       # option nat
    NIL operation; # list operation : option nat
    PAIR;       # pair (list operation) (option nat)
  };